export enum dbKeys {
  user = 'user',
  users = 'users',
  favorites = 'favorites',
  favoriteList = 'list',
  requests = 'requests',
  draft = 'draft',
  published = 'published ',

  profilePics = 'profilePics',

  userConfig = 'config',
}