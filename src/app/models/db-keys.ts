export enum dbKeys {
  user = 'user',
  users = 'users',
  favorites = 'favorites',
  favoriteList = 'list',
  requests = 'requests',
  requestsFollowing = 'requestsFollowing',
  draft = 'draft',
  published = 'published ',

  profilePics = 'profilePics',

  userConfig = 'config',
}